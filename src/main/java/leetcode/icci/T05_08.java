package leetcode.icci;

import java.util.BitSet;

/**
 * @author DubLBo
 * @since 2020-10-27 09:16
 * i believe i can i do
 */
public class T05_08 {
    // 绘制直线。有个单色屏幕存储在一个一维数组中，使得32个连续像素可以存放在一个 int 里。
    // 屏幕宽度为w，且w可被32整除（即一个 int 不会分布在两行上），屏幕高度可由数组长度及屏幕宽度推算得出。
    // 请实现一个函数，绘制从点(x1, y)到点(x2, y)的水平线。

    public static void main(String[] args) {

        System.out.println(Integer.valueOf("FFFF",16).toString());
        System.out.println(Integer.parseInt(String.valueOf(0b11111111111111111111111111111111)));
        System.out.println(Integer.toBinaryString(-1));
    }
}
